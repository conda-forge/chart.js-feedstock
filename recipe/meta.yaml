{% set name = "Chart.js" %}
{% set version = "3.0.1" %}
{% set sha256 = "65143b68880d2eff83e705ea8e4d2defe326d44e108965db183053b7ed2db411" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/chartjs/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: |
    tgz=$(npm pack)
    npm install -g $tgz

requirements:
  build:
    - nodejs

test:
  commands:
    - npm list -g chart.js
  requires:
    - nodejs


about:
  home: https://github.com/chartjs/Chart.js
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: Simple HTML5 Charts using the <canvas> tag
  description: |
    Simple HTML5 Charts using the <canvas> tag  
  doc_url: http://www.chartjs.org/
  dev_url: https://github.com/chartjs/Chart.js

extra:
  recipe-maintainers:
    - cshaley
    - sannykr
